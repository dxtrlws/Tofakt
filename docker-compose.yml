services:
  watchlog:
    image: ghcr.io/dxtrlws/watchlog:latest
    build:
      context: .
      args:
        WATCHLOG_BUILD: ${WATCHLOG_BUILD:-dev}
        WATCHLOG_VERSION: ${WATCHLOG_VERSION:-}
    ports:
      - "9477:9477"
    environment:
      TZ: UTC
      DATABASE_PATH: /data/watchlog.db
      WATCHLOG_GITHUB_TOKEN: ${WATCHLOG_GITHUB_TOKEN:-}
    volumes:
      - watchlog-data:/data
    restart: unless-stopped

volumes:
  watchlog-data:
